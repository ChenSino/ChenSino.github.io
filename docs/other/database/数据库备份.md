---
title: 定时备份数据库
date: 2017-10-08
author: chensino
publish: true
isOriginal: true
---

### 1.备份脚本

脚本文件： xxx.sh

~~~shell
#!/bin/bash

#保存备份个数
number=5
#备份保存路径
backup_dir=/home/chenkun/backup/datbase
#日期
dd=`date +%Y%m%d-%H%M%S`
#备份工具
tool=/usr/bin/mysqldump
# 备份主机
host=127.0.0.1
# mysql服务端口
port=3306
#数据库用户名
username=root
#密码
password=xxx
#将要备份的数据库名字
database_name=xxx

#如果文件夹不存在则创建
if [ ! -d $backup_dir ]; 
then     
    mkdir -p $backup_dir; 
fi

#备份
$tool -h $host -P $port -u $username -p$password $database_name > $backup_dir/$database_name-$dd.sql

#写创建备份日志
echo "【`date +%Y%m%d-%H:%M:%S`】 create $backup_dir/$database_name-$dd.sql" >> $backup_dir/log.txt

#找出需要删除的备份
delfile=`ls -l -crt  $backup_dir/*.sql | awk '{print $9 }' | head -1`

#判断现在的备份数量是否大于$number
count=`ls -l -crt  $backup_dir/*.sql | awk '{print $9 }' | wc -l`

if [ $count -gt $number ]
then
  #删除最早生成的备份，只保留number数量的备份
  rm $delfile
  #写删除文件日志
  echo "【`date +%Y%m%d-%H:%M:%S`】delete $delfile" >> $backup_dir/log.txt
fi

~~~

### 2. 给脚本添加可执行权限

~~~shell
chmod +x xxx.sh
~~~

### 3. 设置crontab定时任务（每天0点执行一次）

~~~shell
crontab -e
~~~

~~~shell
# 脚本路径要使用自己的
0 0 * * * sh /home/data/mysqlbackup/xxx.sh
~~~
